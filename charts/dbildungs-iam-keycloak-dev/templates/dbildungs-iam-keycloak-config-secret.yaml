apiVersion: v1
kind: Secret
metadata:
  name: realm-config-secret
  labels:
    app.kubernetes.io/name: {{.Chart.Name}}
    app.kubernetes.io/version: {{.Chart.Version}}
type: Opaque
data:
  realm-spsh.json: |-
      {{.Files.Get "dev-realm-spsh.json" | replace "___BRANCH_NAME___" .Release.Namespace | b64enc}}
