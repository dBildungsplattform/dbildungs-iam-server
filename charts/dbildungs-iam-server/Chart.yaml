apiVersion: v2
name: dbildungs-iam-server
version: 0.2.0
description: dBildungs-IAM-server
type: application
appVersion: ""
dependencies:
- name: redis-cluster
  repository: https://charts.bitnami.com/bitnami
  version: 10.0.1 # check which image version is used and if we can update further
  condition: redis-cluster.enabled

    # redis:7.2.2

# redis.conf file is used in Helm Chart

