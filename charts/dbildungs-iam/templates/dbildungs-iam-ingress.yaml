apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{.Release.Name}}-backend
  labels:
    app.kubernetes.io/name: dbildungs-iam
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: {{.Values.dbildungsIamBackendHost}}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: dbiam
                port:
                  number: 80
                