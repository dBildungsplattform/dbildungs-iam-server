apiVersion: v1
kind: Secret
metadata:
  name: realm-config-secret
  labels:
    app.kubernetes.io/name: {{.Chart.Name}}
    app.kubernetes.io/version: {{.Chart.Version}}
type: Opaque
data:
  realm-spsh.json: |-
      {{.Files.Get "dev-realm-spsh.json" | replace "BRANCH_NAME.dev.spsh.dbildungsplattform.de" .Values.global.backendHostname | b64enc}}
