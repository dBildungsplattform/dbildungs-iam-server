apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbildungs-iam-redis.fullname" . }}
  labels:
    {{- include "dbildungs-iam-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbildungs-iam-redis.selectorLabels" . | nindent 4 }}
