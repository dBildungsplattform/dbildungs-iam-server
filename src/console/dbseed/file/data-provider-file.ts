/* v8 ignore file @preserv */
// DataProviders are only used in seeding and not in production. It is not absolutely necessary to test them.
import { DataProviderEntity } from '../../../persistence/data-provider.entity.js';

export class DataProviderFile extends DataProviderEntity {}
